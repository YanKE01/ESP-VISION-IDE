[project]
name = "viperide"
version = "0.5.2"
description = "An innovative MicroPython / CircuitPython IDE for Web and Mobile"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
]

[tool.uv]
package = false
